package ${basePackageDao};

import com.anka.base.mapper.CrudBaseMapper;
import ${basePackageModel}.${claszName};

/**
 * @Description:${reMarks!''}(${claszName}Mapper)数据持久层接口
 * @author ${author}
 * @date ${date}
 * @version ${version}
 */
public interface ${claszName}Mapper extends CrudBaseMapper<${claszName}>{
	
}
